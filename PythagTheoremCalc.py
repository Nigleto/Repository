question = 'yes'
while (question == 'yes'):
  epic = input("Enter an a.")
  epicc = input("Enter a B.")
  answer = float(epic)*float(epic) + float(epicc)*float(epicc)
  print(answer)
  question = input('Would you like to do this again?')
  
